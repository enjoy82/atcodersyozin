# include<iostream>
# include<vector>
#include<algorithm>
#include<cstdlib>
#include <math.h>
using namespace std;

int main(){
    int n; cin >> n;
    vector<int> a(n,0);
    vector<int> b(n,0);
    vector<int> ans(n,0);
    for(int i =0; i<n; i++){
        int mid
        cin >> mid 
        a[i] = mid;b[i] = mid;
    }
    for(int i = 0; i < n; i++){
        
    }
}