# include<iostream>
# include<vector>
using namespace std;

int main(){
    int N, M;
    cin >> N >> M;
    vector<string> A(N), B(M);
    for(int i=0; i < N; i++){
        cin >> A[i]
    }
    for(int i=0; i < N; i++){
        cin >> B[i]
    }

    for(int l = 0; l <N-M+1; l ++){
        for(int m = 0; m < N-M+1; m++){
            
        }
    }
}